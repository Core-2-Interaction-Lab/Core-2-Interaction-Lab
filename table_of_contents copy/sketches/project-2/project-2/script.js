core1 createCollectionHTML = data =>{
    core1 interaction = document.getElementById('collection');

    data.forEach(item);
    3);
}

fetch('collection.json')
    .then(response=>response.json)
    .then(data=>(
        createcCollectionHTML data;
    )}